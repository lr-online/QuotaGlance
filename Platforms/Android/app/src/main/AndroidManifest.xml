<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:name=".refresh.QuotaGlanceApplication"
        android:allowBackup="false"
        android:icon="@drawable/quotaglance_icon"
        android:label="QuotaGlance"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="quotaglance" />
            </intent-filter>
        </activity>
        <activity
            android:name=".widget.QuotaGlanceWidgetConfigurationActivity"
            android:exported="false" />
        <receiver
            android:name=".widget.QuotaGlanceWidgetReceiver"
            android:exported="true"
            android:label="QuotaGlance">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quotaglance_widget_info" />
        </receiver>
    </application>
</manifest>
